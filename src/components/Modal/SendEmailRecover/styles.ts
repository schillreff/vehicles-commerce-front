import styled from 'styled-components';

export const StyledSendEmailRecover = styled.div`
  display: flex;
  width: 100%;
  .send-email-form {
    min-width: 100%;
    margin-top: 20px;
    padding: 0;
    .send-email-form__buttons {
      display: flex;
      align-self: flex-end;
      gap: 16px;
    }
  }
`;
